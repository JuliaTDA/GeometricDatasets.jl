# Preface {.unnumbered}

This is a Quarto book.

To learn more about Quarto books visit <https://quarto.org/docs/books>.


```{julia}
using GeometricDatasets
using Plots
```


```{julia}
X = sphere(1000; radius = 100, noise = x -> randn(x) * 10)
scatter(X[1, :], X[2, :])
```


```{julia}
X = sphere(1000, dim = 3, radius = 10, noise = randn)
scatter(X[1, :], X[2, :], X[3, :], alpha = 0.5)
```